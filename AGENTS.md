# 仓库协作约定

本仓库用于研究多个 GitHub 开源项目。默认使用中文撰写说明，具体约定见 `docs/CONVENTIONS.md`。

- 新研究放在 `projects/<至少三位编号>-<英文短名>/`，参考 `templates/project/`。
- 编号从 `001` 递增，已分配编号保持不变且不复用；根索引和图览按编号数值升序维护。
- 根 `README.md` 只维护摘要、索引和代表图片；详细研究与运行说明留在子项目中。
- 新增项目或改变研究进度、演示地址时，同步维护根索引和对应子项目说明。
- 记录上游仓库、研究版本和许可证，保留引用代码与素材所需的版权声明。
- 不把占位图、规划中的地址或尚未执行的步骤描述为真实截图、已部署结果或已验证结论。
- 每个子项目按实际需求选择技术栈；未经需要，不引入统一的根应用或跨项目依赖。
- Web 演示遵循 `docs/DEPLOYMENT.md`，多个静态演示使用各自编号子路径。
- 修改时遵循子项目内更具体的协作规则，执行与修改范围相适应的检查。
- 不提交密钥、本地环境、依赖缓存或无关构建产物。
