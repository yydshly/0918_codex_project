const main = document.querySelector('main');
let category='全部', search='', selectedCap='design', step=0, scenario='landing';
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const heading=(label,title,desc)=>`<div class="eyebrow">${label}</div><h1>${title}</h1><p class="lead">${desc}</p>`;
function overview(){return heading('一图理解 · 003','Claude Code 前端设计与实现指南','围绕前端需求，从视觉风格、主题一致性、动效交互、用户体验与可访问性、设计稿衔接、框架与文档、浏览器验证及预览交付等角度，组织约束、方法和工具，指导 Agent 设计、实现与检查页面，以获得更符合需求的效果。')+`<section class="understanding-intro" aria-label="先理解三件事"><article><h2>约束 Agent 怎样做</h2><p>围绕具体需求，组织设计原则、项目规范、实现步骤与验收要求，减少随意选择与遗漏。</p></article><article><h2>用工具落实与检查</h2><p>按需接入设计稿、框架文档和浏览器，让 Agent 根据真实资料实现，并检查运行结果。</p></article><article><h2>对我们的实际价值</h2><p>现有 Agent 已会的通用方法，增益可能有限。重点补充项目规范、资料和检查能力。</p><a href="#practice">看需求如何结合指导 ↗</a></article></section><div class="map-toolbar" role="group" aria-label="总览图查看方式"><button class="button" id="map-minus" aria-label="缩小总览图">缩小 −</button><button class="button" id="map-plus" aria-label="放大总览图">放大 +</button><button class="button" id="map-fit">适应宽度</button><span id="map-scale" aria-live="polite">100%</span><a href="../assets/understanding-map.png" target="_blank" rel="noopener">打开高清图 ↗</a><a href="../assets/understanding-map.svg" target="_blank" rel="noopener">矢量原图 ↗</a></div><figure class="map-figure"><div class="map-frame" id="map-frame" tabindex="0" role="region" aria-label="能力总览图，可放大后滚动查看"><img id="map-image" src="../assets/understanding-map.svg" alt="指导 Claude Code 做好前端设计与实现：九个工作方面、任务与工具的分工、对我们的意义和能力边界"></div><figcaption>原创概念总览，非上游产品截图。九项按原目录展开；下方八类能力地图将文档与框架知识合并。<a href="../notes/research.md">阅读文字分析</a></figcaption></figure><div class="guide-links"><a class="button primary" href="#practice">这个库的实际价值</a><a class="button" href="#capabilities">展开能力细节</a><a class="button" href="#mechanism">体验原理演示</a><a class="button" href="#scenarios">按场景选型</a><a class="button" href="#evidence">回查证据边界</a></div><div class="note"><strong>这是 Agent 的工作指导：</strong>它提供的是 Agent 工作方式的指导与资源索引：Skill 和项目规则约束决策，外部工具提供资料与操作，浏览器反馈帮助修正。模型基础能力没有因此被训练或升级，效果仍取决于实际执行。 该研究版本只有 README，具体指导或操作来自它推荐的独立资源；本研究未证明生成质量提升。</div>`;}
let mapZoom=1;
function resizeMap(action){mapZoom=action==='fit'?1:Math.max(.5,Math.min(3,mapZoom+(action==='plus'?.25:-.25)));const frame=document.querySelector('#map-frame');document.querySelector('#map-image').style.width=`${mapZoom*100}%`;frame.dataset.zoomed=String(mapZoom>1);document.querySelector('#map-scale').textContent=`${Math.round(mapZoom*100)}%`;document.querySelector('#map-minus').disabled=mapZoom<=.5;document.querySelector('#map-plus').disabled=mapZoom>=3;if(action==='fit'){frame.scrollTop=0;frame.scrollLeft=0;}}
document.addEventListener('click',event=>{const b=event.target.closest('button');if(b&&['map-plus','map-minus','map-fit'].includes(b.id))resizeMap(b.id.replace('map-',''));});
function capabilities(){return heading('能力地图','从任务出发，看工具的分工。','选择一种能力，查看它接收什么、产出什么，以及需要额外验证的部分。以下为研究归纳，非已安装工具列表。')+`<div class="toolbar" role="group" aria-label="能力筛选">${['全部','设计','实现','验证','交付'].map(g=>`<button class="filter" data-filter="${g}" aria-pressed="${g===category}">${g}</button>`).join('')}<input id="search" type="search" placeholder="搜索能力或工具" aria-label="搜索能力或工具" value="${esc(search)}"></div><div class="cap-layout"><div><p id="result-count" aria-live="polite"></p><div id="cap-grid" class="cap-grid"></div></div><article class="detail" id="cap-detail" aria-live="polite"></article></div>`;}
function drawCaps(){const list=CAPS.filter(c=>(category==='全部'||c.group===category)&&`${c.title} ${c.tools} ${c.short}`.toLowerCase().includes(search.toLowerCase()));if(!list.some(c=>c.id===selectedCap))selectedCap=list[0]?.id;document.querySelector('#result-count').textContent=`显示 ${list.length} 项能力`;document.querySelector('#cap-grid').innerHTML=list.length?list.map(c=>`<button class="cap" data-cap="${c.id}" aria-pressed="${selectedCap===c.id}"><small>${c.group}</small><h3>${c.title}</h3><p>${c.short}</p></button>`).join(''):'<p class="empty">没有匹配的能力。请更换关键词或选择“全部”。</p>';const c=CAPS.find(c=>c.id===selectedCap);document.querySelector('#cap-detail').innerHTML=c?`<span class="pill">${c.evidence}</span><h2>${c.title}</h2><p>${c.tools}</p><dl><dt>输入</dt><dd>${c.input}</dd><dt>产出</dt><dd>${c.output}</dd><dt>实现机制</dt><dd>${c.mechanism}</dd></dl><div class="boundary"><strong>能力边界</strong>${c.limit}</div><p style="margin:18px 0 0"><a href="${sourceURL(c.source)}">回查固定版本来源 ↗</a></p>`:'<h2>等待选择</h2><p>筛选结果为空时不展示上一项内容。</p>';}
function mechanism(){return heading('原理与演示','把规则、工具和反馈连起来。','下面用“制作科研工具介绍页”解释典型工作流程。内容为预设教学示意，不会发起模型、网络或第三方工具调用。')+`<div class="flow"><div class="steps" aria-label="流程步骤">${STEPS.map((s,i)=>`<button class="step" data-step="${i}" ${i===step?'aria-current="step"':''}><span>${i+1}</span>${s.title}</button>`).join('')}</div><article class="flow-panel" id="flow-detail" aria-live="polite"></article></div><div class="section-head"><h2>动手理解：设计变量如何统一组件</h2><span class="pill">本地真实 CSS 演示</span></div><div class="lab"><div><h3>一个色相，三个关联元素</h3><p>拖动滑块，标题、标签和按钮样式同步变化。这里用 HSL 简化说明变量复用；不代表上游 OKLCH 工具的输出。</p><label for="hue">品牌色相 <output id="hue-value">215°</output></label><input id="hue" type="range" min="0" max="360" value="215"><code class="code-value" id="token-code">--hue: 215;</code></div><div class="token-preview" id="token-preview"><span class="sample-badge">研究笔记</span><h3>把新的发现，记录下来。</h3><p>这个小样板展示共享变量的作用。页面内容与布局由本研究编写。</p><div class="sample-line"></div><span class="sample-cta">阅读笔记 · 样式示意</span></div></div>`;}
function drawStep(){const s=STEPS[step];document.querySelectorAll('[data-step]').forEach(b=>{if(Number(b.dataset.step)===step)b.setAttribute('aria-current','step');else b.removeAttribute('aria-current');});document.querySelector('#flow-detail').innerHTML=`<span class="pill">${s.actor}</span><h2>${s.title}</h2><p>${s.action}</p><div class="io"><div><small>本步骤输入</small><p>${s.input}</p></div><div><small>本步骤产出</small><p>${s.output}</p></div></div><div class="boundary"><strong>此处要注意</strong>${s.boundary}</div><div class="flow-controls"><button class="button" id="prev" ${step===0?'disabled':''}>上一步</button><button class="button primary" id="next" ${step===STEPS.length-1?'disabled':''}>下一步</button><button class="button" id="reset">重置</button><small>步骤 ${step+1} / ${STEPS.length}</small></div>`;}
function scenarios(){return heading('场景选型','补齐眼前缺口，再考虑组合。','以下是基于工具职责提出的研究建议。使用前核对各工具的当前文档；页面不会替你安装工具。')+`<div class="scenarios" role="group" aria-label="选择使用场景">${SCENARIOS.map(s=>`<button class="choice" data-scenario="${s.id}" aria-pressed="${s.id===scenario}"><small>${s.need}</small><strong>${s.title}</strong></button>`).join('')}</div><article id="scenario-detail" class="scenario-result" aria-live="polite"></article><div class="note"><strong>对当前研究仓库的价值：</strong>可借鉴“设计要求 → 实现 → 浏览器核查”的方法；将风格落实为共享变量，把实际截图与验证边界一起保留。无需把这个 README 仓库作为运行依赖。</div>`;}
function drawScenario(){const s=SCENARIOS.find(s=>s.id===scenario);document.querySelectorAll('[data-scenario]').forEach(b=>b.setAttribute('aria-pressed',b.dataset.scenario===scenario));document.querySelector('#scenario-detail').innerHTML=`<div><span class="pill">${s.need}</span><h2 style="margin-top:18px">${s.title}</h2><div class="stack">${s.stack.map(x=>`<span>${x}</span>`).join('')}</div><h3>组合理由</h3><p>${s.reason}</p><a href="${UPSTREAM}#recommended-stacks">对照上游组合建议 ↗</a></div><div class="verdict"><h3>怎样才算完成？</h3><p>${s.accept}</p><p class="avoid">常见误区：${s.avoid}</p></div>`;}
function evidence(){return heading('证据与边界','看过什么，验证到哪一步。','核查日期：2026-09-18。上游版本与第三方资料分别固定，避免把后续变动混入本轮结论。')+`<div class="audit-grid"><article><span class="pill green">可回查</span><h3 style="margin-top:15px">本轮已完成的核查</h3><ul><li>Toolkit 文件树与 README 定位</li><li>设计指令、文档检索和浏览器接口说明</li><li>独立展厅的交互与响应式检查</li></ul><a href="../notes/evidence/browser-qa.json">展厅检查记录 ↗</a></article><article><span class="pill">尚未验证</span><h3 style="margin-top:15px">这些结论不能从展厅推出</h3><ul><li>模型实际生成质量有多大提升</li><li>全部工具的安装兼容与账户权限</li><li>Figma、部署服务和完整工具组合</li></ul><p>流程为教学示意；样式实验只验证 CSS 变量。</p></article></div><div class="section-head"><h2>五份核心资料</h2><a href="../notes/evidence/sources.json">来源元数据 ↗</a></div><div class="evidence-list">${SOURCES.map(s=>`<article class="source-row"><div><h3>${s.name}</h3><a href="${sourceURL(s.id)}"><code>${s.sha.slice(0,12)}</code> · 查看来源 ↗</a></div><p>${s.finding}</p></article>`).join('')}</div><div class="audit-grid"><article><h3>安装信息需要回到官方核对</h3><p>Toolkit 的 Chrome DevTools 条目写成 Anthropic，并使用带 @anthropic-ai 作用域的包名。官方来源是 ChromeDevTools/chrome-devtools-mcp，安装示例使用 chrome-devtools-mcp。本研究不复制该错误命令。</p></article><article><h3>许可声明存在缺口</h3><p>README 声明 MIT 并链接 LICENSE；固定版本的文件树中没有该文件。本项目只记录来源与原创分析，没有复制上游工具代码；第三方资源各自适用其许可。</p></article></div><div class="note"><strong>不要把宣传数字当测量结果：</strong>README 中的工具数量、Stars 和上下文开销未在此逐项复测。它们不用于本研究的能力评分，也不能证明生成效果。</div><div class="doc-links"><a href="../README.md">项目说明 ↗</a><a href="../notes/research.md">完整研究 ↗</a><a href="../notes/usage.md">使用与选型指南 ↗</a><a href="../THIRD_PARTY_NOTICES.md">来源与许可说明 ↗</a></div>`;}
const views={overview,capabilities,mechanism,practice,scenarios,evidence};
function render(){const route=location.hash.slice(1)||'overview';const key=views[route]?route:'overview';main.innerHTML=views[key]();if(key==='overview')mapZoom=1;document.querySelectorAll('[data-page]').forEach(a=>{if(a.dataset.page===key)a.setAttribute('aria-current','page');else a.removeAttribute('aria-current');});document.title=`${document.querySelector(`[data-page="${key}"]`).textContent.trim()} · Frontend Design Toolkit`;if(key==='capabilities')drawCaps();if(key==='mechanism')drawStep();if(key==='scenarios')drawScenario();if(key==='practice')drawPractice();}
document.addEventListener('click',event=>{const b=event.target.closest('button');if(!b)return;if(b.dataset.filter){category=b.dataset.filter;document.querySelectorAll('[data-filter]').forEach(x=>x.setAttribute('aria-pressed',x.dataset.filter===category));drawCaps();}if(b.dataset.cap){selectedCap=b.dataset.cap;drawCaps();document.querySelector(`[data-cap="${selectedCap}"]`).focus();}if(b.dataset.step!==undefined){step=Number(b.dataset.step);drawStep();}if(b.id==='next'||b.id==='prev'||b.id==='reset'){step=b.id==='reset'?0:Math.max(0,Math.min(STEPS.length-1,step+(b.id==='next'?1:-1)));const id=b.id;drawStep();const target=document.getElementById(id);(target.disabled?document.getElementById(id==='next'?'prev':'next'):target).focus();}if(b.dataset.scenario){scenario=b.dataset.scenario;drawScenario();}});
document.addEventListener('input',event=>{if(event.target.id==='search'){search=event.target.value;drawCaps();}if(event.target.id==='hue'){const hue=event.target.value;document.querySelector('#hue-value').textContent=`${hue}°`;document.querySelector('#token-code').textContent=`--hue: ${hue};`;document.querySelector('#token-preview').style.setProperty('--hue',hue);}});
addEventListener('hashchange',()=>{render();scrollTo(0,0);main.focus({preventScroll:true});});render();
