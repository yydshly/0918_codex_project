# Order Lab · 解析实验样本

本研究原创的最小 TypeScript 样本：入口 → 订单编排 → 库存 / 模拟支付 / 模拟保存。

只有源码解析所需的几个函数，不是生产应用。支付和保存只返回字符串，不发起支付、不连接数据库。`schema.sql` 用于独立验证 SQL 解析，不声称代码已访问该表。

库存函数的 `100 → 10` 修改用于验证：业务行为改变时，上游结构指纹是否能够识别。
